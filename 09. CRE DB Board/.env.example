# Server-side only. Never prefix database credentials with NEXT_PUBLIC_.
SUPABASE_DB_URL=
DASHBOARD_ACCESS_CODE=
DASHBOARD_SESSION_SECRET=

# Optional local alternative; use an absolute path outside the repository.
SUPABASE_ENV_FILE=
